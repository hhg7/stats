aoh2hoh
hoa2hoh
ncol
nrow
`value_counts` should accept array of hashes
`summary` should have colored and tidier output like new `view` does
`view` can accept hashes and arrays without needing a reference
`to_list` or `col2arr` or `vals` like Pandas
