aoh2hoh
hoa2hoh
`csort` should accept HoH, but cannot return HoH as hashes aren't sorted; output type should be specified as either `hoa` or `aoh` (matching provided by default); undefined values should be at the end
ncol
nrow
`value_counts` should accept array of hashes
`summary` should have colored and tidier output like new `view` does
`view` can accept hashes and arrays without needing a reference
`to_list` or `col2arr` or `vals` like Pandas
