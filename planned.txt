aoh2hoh
hoa2hoh
`filter` should work with HoH
`filter` shouldn't need `col()` if appropriate, ask Claude
`filter` should have an output type option
`filter` is not filtering correctly: filter( $f, 295 < col('crystal_temp')) where $f is AoH
ncol
nrow
`view` should display `undef` rather than `NA`
`value_counts` should accept array of hashes
`summary` should have colored and tidier output like new `view` does
